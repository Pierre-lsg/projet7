<form method="post" action="{{ path('app_repere_delete', {'id': repere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repere.id) }}">
    <button class="btn">Delete</button>
</form>
