<header class="container">
    <hgroup>
        <h1><a class="contrast" href="{{ path('app_accueil') }}">Projet 7</a></h1>
        <p>Streetgolfeur un jour, StreetGolf pour toujours ...</p>
    </hgroup>
    <nav>
        <ul>
            <li><strong><a class="contrast" href="{{ path('app_accueil') }}">Projet 7</a></strong></li>
        </ul>
        <ul>
            {% if is_granted("ROLE_USER") %}
                <li><a class="contrast" href="{{ path('app_logout') }}">Déconnexion</a></li>
            {% else %}
                <li><a class="contrast" href="{{ path('app_login') }}">Connexion</a></li>
            {% endif %}
        </ul>
    </nav>
</header>
