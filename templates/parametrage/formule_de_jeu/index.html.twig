{% extends 'base.html.twig' %}

{% block title %}FormuleDeJeu index{% endblock %}

{% block body %}
    <h1>FormuleDeJeu index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formule_de_jeu in formule_de_jeus %}
            <tr>
                <td>{{ formule_de_jeu.nom }}</td>
                <td>{{ formule_de_jeu.description }}</td>
                <td>
                    <a role="button" href="{{ path('app_fdj_show', {'id': formule_de_jeu.id}) }}">show</a>
                    <a role="button" href="{{ path('app_fdj_edit', {'id': formule_de_jeu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fdj_new') }}">Create new</a>
    <a href="{{ path('app_parametrage') }}">Back to param index</a>
{% endblock %}
