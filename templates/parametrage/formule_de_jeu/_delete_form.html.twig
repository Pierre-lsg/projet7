<form method="post" action="{{ path('app_fdj_delete', {'id': formule_de_jeu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule_de_jeu.id) }}">
    <button class="btn">Delete</button>
</form>
