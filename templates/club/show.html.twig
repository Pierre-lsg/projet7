{% extends 'base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ club.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ club.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ club.description }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" href="{{ path('app_club_index') }}">Retour à la liste des clubs</a>

    <a role="button" href="{{ path('app_club_edit', {'id': club.id}) }}">éditer</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
